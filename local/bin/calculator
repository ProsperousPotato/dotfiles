#!/bin/sh

input=$(cat ~/.local/share/qalculate/qalc_history | dmenu -p "Calculation: ")

[ "$input" == "exit" ] && exit 1
[ -n "$input" ] && result=$(qalc "$input") ; echo "$result" | dmenu -p "Answer:" && echo "$result" >> ~/.local/share/qalculate/qalc_history
